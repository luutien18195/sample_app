<li>
  <%= gravatar_for user, size: Settings.size %>
  <%= link_to user.name, user %>
  <% if current_user.is_admin? && !user.current_user?(current_user) %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: {confirm: t("users.user_html.you_sure")} %>
  <% end %>
</li>
